# DNS Adapter Boundary

Magnanimous AI owns orchestration, memory, governance, and public identity. DNS resolvers and registrar APIs are replaceable adapters. Provider credentials stay server-side, and consequential DNS writes remain approval-gated.

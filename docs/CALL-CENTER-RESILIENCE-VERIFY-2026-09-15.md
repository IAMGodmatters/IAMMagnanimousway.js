# Cost-first call center + Agent Mesh resilience verification

This verification marker triggers the standard production pipeline after the call-center cost-first architecture, provider-private customer surfaces, Agent Desk script/rebuttal workflow, BYOC routing support, and Agent Mesh local capacity-resilience fallback were locked into the current source tree.

The intended route order remains: free browser calling first, workspace BYOC/flat-rate or wholesale SIP where permitted, low-cost metered overflow, then premium carrier fallback. Third-party execution-provider identities remain private on customer-facing surfaces.

Agent Mesh still prefers its configured AI execution routes. The local resilience responder is only a truthful zero-cost continuity fallback when all configured reasoning providers fail or are temporarily capacity-limited.

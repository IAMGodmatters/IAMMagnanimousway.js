# Multi-Video Agents Release

Adds six specialist video-agent roles, a free-first storyboard/scene-generation API, a dedicated Video Agents dashboard, global navigation, provider readiness for optional HeyGen/Tavus presentation paths, and CI syntax coverage for the new runtime.

The release intentionally keeps ordinary video creation independent from premium avatar providers. Premium providers remain optional and are only reported as ready when their credentials are configured.

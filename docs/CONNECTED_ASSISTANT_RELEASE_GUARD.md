# Connected Assistant release guard

Do not merge this branch unless exact-head pull-request checks are green. After merge, production completion must be proven by deployment logs and smoke tests rather than inferred from source state.

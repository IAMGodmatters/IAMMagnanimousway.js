# Magnanimous Dev Agent PR scope

This change adds an original Magnanimous-native developer-agent layer. It does not copy proprietary Codex source or claim to embed Codex.

The change is additive: existing platform, AI, telecom, Shopify, Tool Foundry, specialist-agent, security, and deployment behavior is preserved.

Merge gate: exact-head CI must pass. Production completion requires the post-merge deployment and smoke test to pass. Repository write automation remains fail-closed until a dedicated server-side GitHub credential is configured.

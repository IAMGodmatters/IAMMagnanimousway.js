import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..'),runtime=fs.readFileSync(path.join(root,'worker/src/magnanimous-business-ai-suite.js'),'utf8');const routes=[...runtime.matchAll(/surface:'([^']+)'/g)].map(m=>m[1]);if(routes.length!==31)throw Error('expected 31 Business AI surfaces, found '+routes.length);for(const route of routes){const pathname=route.split('?')[0];if(!pathname.startsWith('/'))throw Error('surface must be internal: '+route);const seg=pathname.replace(/^\//,'');const file=path.join(root,'frontend/app',seg,'page.tsx');if(!fs.existsSync(file))throw Error('Business AI surface does not exist: '+route+' -> '+file)}console.log('Business AI surface route integrity passed for '+routes.length+' capabilities.');
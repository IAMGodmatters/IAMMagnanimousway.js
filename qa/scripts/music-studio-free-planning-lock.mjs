import fs from'node:fs';const x=fs.readFileSync(new URL('../../worker/src/music-studio.js',import.meta.url),'utf8'),p=fs.readFileSync(new URL('../../frontend/app/music-studio/page.tsx',import.meta.url),'utf8');for(const s of ["owner_user_id=? ORDER BY","project(env,tid,user.id","generation plan is required","if(opm&&request.method==='POST')"])if(!x.includes(s))throw Error('music access/isolation safeguard missing '+s);const gate=x.indexOf("generation plan is required"),projectPost=x.indexOf("path==='/api/music/projects'&&request.method==='POST'");if(gate<projectPost)throw Error('music entitlement still blocks free project planning');if(!p.includes('Project planning, briefs and saved original-work projects are available without a generation subscription'))throw Error('music free-first UI copy missing');console.log('Music Studio free planning, paid generation, and user isolation passed.');
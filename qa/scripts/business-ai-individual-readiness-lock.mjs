import fs from'node:fs';const r=p=>fs.readFileSync(new URL('../../'+p,import.meta.url),'utf8'),x=r('worker/src/magnanimous-business-ai-suite.js'),p=r('frontend/app/business-ai/page.tsx');const ids=[...x.matchAll(/\['([^']+)','[^']+','[^']+','[^']+'\]/g)].map(m=>m[1]);for(const id of ids){if(!x.includes("'"+id+"':{surface:"))throw Error('missing capability route '+id)}for(const s of ['accessibility_standard','truth_boundary','dependencies'])if(!x.includes(s))throw Error('missing readiness contract '+s);if(!p.includes('Open its working surface'))throw Error('missing surface navigation');console.log('Business AI individual capability readiness passed for '+ids.length+' tools.');
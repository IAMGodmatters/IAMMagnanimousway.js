import fs from'node:fs';const x=fs.readFileSync(new URL('../../worker/src/magnanimous-business-ai-suite.js',import.meta.url),'utf8');const all=[...x.matchAll(/\['([^']+)','[^']+','[^']+','[^']+'\]/g)].map(m=>m[1]);for(const id of all){if(id==='hyper-images'){if(!x.includes("row.tool_id==='hyper-images'"))throw Error('missing specialized image execution');continue}if(!x.includes("'"+id+"':{tool:"))throw Error('missing direct execution adapter '+id)}if(!x.includes("'crm':{tool:'business'"))throw Error('CRM adapter missing');if(!x.includes("'app-wizard':{tool:'coding'"))throw Error('App Wizard adapter missing');console.log('Complete Business AI adapter matrix passed for '+all.length+' capabilities.');
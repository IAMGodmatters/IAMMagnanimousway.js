name: Repair Agent Mesh provider timeouts

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Apply Agent Mesh timeout repair
        run: node scripts/repair-agent-mesh-timeouts.mjs

      - name: Verify Worker syntax
        run: node --check worker/src/agent-mesh-runtime.js

      - name: Commit repair
        run: |
          if git diff --quiet -- worker/src/agent-mesh-runtime.js; then
            echo "Agent Mesh timeout repair already present."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add worker/src/agent-mesh-runtime.js
          git commit -m "Bound Agent Mesh provider latency and failover"
          git push origin HEAD:main

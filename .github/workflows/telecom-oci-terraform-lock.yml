name: Magnanimous OCI Terraform Free Guard

on:
  push:
    branches: ['**']
    paths:
      - 'telecom-core/oci/**'
      - '.github/workflows/telecom-oci-terraform-lock.yml'
  pull_request:
    paths:
      - 'telecom-core/oci/**'
      - '.github/workflows/telecom-oci-terraform-lock.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: oci-terraform-free-guard-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.13'

      - name: Test OCI plan guard
        working-directory: telecom-core/oci
        run: python -m unittest -v test_verify_free_plan.py

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: 1.16.4
          terraform_wrapper: false

      - name: Validate Cloud Shell helper syntax
        run: bash -n telecom-core/oci/cloud-shell-plan.sh

      - name: Lock Cloud Shell helper safety behavior
        shell: bash
        run: |
          set -euo pipefail
          grep -q 'MODE="plan"' telecom-core/oci/cloud-shell-plan.sh
          grep -q '"--apply"' telecom-core/oci/cloud-shell-plan.sh
          grep -q 'python3 verify_free_plan.py tfplan.json' telecom-core/oci/cloud-shell-plan.sh
          grep -q 'No resources were created.' telecom-core/oci/cloud-shell-plan.sh
          grep -q 'ADMIN_SSH_CIDR is required' telecom-core/oci/cloud-shell-plan.sh
          if grep -q 'terraform apply -auto-approve' telecom-core/oci/cloud-shell-plan.sh; then
            echo 'Cloud Shell helper must never auto-approve an apply.' >&2
            exit 1
          fi

      - name: Terraform format check
        run: terraform fmt -check -recursive telecom-core/oci

      - name: Initialize OCI provider without backend
        run: terraform -chdir=telecom-core/oci init -backend=false -input=false

      - name: Validate guarded OCI module
        run: terraform -chdir=telecom-core/oci validate

      - name: Lock Terraform module safety contracts
        shell: bash
        run: |
          set -euo pipefail
          grep -q 'VM.Standard.A1.Flex' telecom-core/oci/main.tf
          grep -q 'ocpus          = 2' telecom-core/oci/main.tf
          grep -q 'memory_gib     = 12' telecom-core/oci/main.tf
          grep -q 'boot_volume_gb = 50' telecom-core/oci/main.tf
          grep -q 'assign_public_ip = true' telecom-core/oci/main.tf
          grep -q 'admin_ssh_cidr' telecom-core/oci/main.tf
          grep -q 'MAX_BOOT_VOLUME_GB = 50' telecom-core/oci/verify_free_plan.py
          if grep -R --line-number -E 'VM.Standard[0-9]|VM.DenseIO|BM\.|oci_core_load_balancer|oci_database_' telecom-core/oci --include='*.tf'; then
            echo 'Unexpected paid compute/database/load-balancer construct found in guarded OCI Terraform source.' >&2
            exit 1
          fi

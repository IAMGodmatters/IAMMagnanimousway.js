{
  "private": true,
  "name": "magnanimous-native-webrtc-e2e",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && esbuild client.js --bundle --format=iife --platform=browser --outfile=dist/client.js && cp index.html dist/index.html",
    "probe": "node run.mjs"
  },
  "dependencies": {
    "@playwright/test": "1.63.0",
    "esbuild": "0.24.2",
    "sip.js": "0.21.2"
  }
}

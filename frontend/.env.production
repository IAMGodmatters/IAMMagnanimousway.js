# Production serves the Next.js assets and Worker API from the same branded origin.
# Keep this empty so browser requests use relative /api routes and never expose or
# depend on the disabled workers.dev development hostname.
NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_VIDEO_API_BASE_URL=https://iam-magnanimous-video-gateway.iam-magnanimous.workers.dev

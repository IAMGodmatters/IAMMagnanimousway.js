'use client';
import {useState} from 'react';
const api=process.env.NEXT_PUBLIC_API_BASE_URL||'';
export default function VideoStudio(){const[text,setText]=useState('Faith can move mountains. Keep believing, keep praying, and keep moving forward.'),[title,setTitle]=useState('I AM Magnanimous Way™'),[url,setUrl]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');async function render(){setBusy(true);setUrl('');setError('');try{const r=await fetch(`${api}/api/video/render`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text,title})});const d=await r.json();if(d.download_url)setUrl(`${api}${d.download_url}`);else setError(d.detail||'Video renderer is not available.')}catch{setError('Unable to reach the video renderer.')}finally{setBusy(false)}}return <main className="module-page"><header className="module-header"><a href="/">← Back to I AM Magnanimous</a><span className="module-status">● VIDEO ENGINE</span></header><section className="module-hero"><div className="module-icon">▶</div><div><span className="eyebrow">CREATOR STUDIO</span><h1>Video Studio</h1><p>Create a video from your message using the existing free rendering pipeline, or connect your own Mux account for hosted video uploads and playback.</p><div style={{display:'flex',gap:10,flexWrap:'wrap',marginTop:14}}><a href="/mux" style={{display:'inline-block',padding:'11px 15px',borderRadius:10,textDecoration:'none',background:'linear-gradient(90deg,#00b9e8,#6b62ff)',color:'#fff',fontWeight:800}}>Open Mux Video →</a><a href="/connections" style={{display:'inline-block',padding:'11px 15px',borderRadius:10,textDecoration:'none',border:'1px solid rgba(120,150,255,.35)',color:'inherit'}}>Connections</a></div></div></section><section className="video-studio-grid"><div className="module-panel"><div className="panel-title"><span>VIDEO INPUT</span><b>FFMPEG</b></div><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Video title"/><textarea value={text} onChange={e=>setText(e.target.value)}/><button onClick={render} disabled={busy}>{busy?'Rendering…':'Create Free Video →'}</button>{error&&<div className="error">{error}</div>}</div><div className="video-preview"><div className="scanlines"/><div className="preview-orb">✦</div><span>LIVE PREVIEW</span>{url?<video controls src={url}/>:<p>Your rendered video will appear here.</p>}</div></section></main>}

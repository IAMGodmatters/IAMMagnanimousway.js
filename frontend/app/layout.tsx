import React from 'react';

export default function RootLayout({children}:{children:React.ReactNode}){
  return <html lang="en"><body>{children}<script dangerouslySetInnerHTML={{__html:`(function(){var done=false;function go(){if(done)return;try{var p=window.location.pathname;var publicAuth=p==='/signup'||p==='/login';var customer=localStorage.getItem('iam_account_token');var owner=localStorage.getItem('odin_admin_token');if(p==='/'&&!publicAuth&&!customer&&!owner){done=true;window.location.replace('/signup');return;}if(owner&&p==='/' ){var buttons=Array.from(document.querySelectorAll('button'));var b=buttons.find(function(x){return (x.textContent||'').trim()==='Admin & Revenue';});if(b){done=true;b.click();}}}catch(e){}}window.addEventListener('load',go);go();setInterval(go,500);})()`}} /></body></html>
}
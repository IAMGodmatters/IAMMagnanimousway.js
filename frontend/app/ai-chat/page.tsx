'use client';
import {useState} from 'react';
const api=process.env.NEXT_PUBLIC_API_BASE_URL||'';
export default function AIChat(){const [message,setMessage]=useState(''),[output,setOutput]=useState('');async function ask(){if(!message.trim())return;setOutput('Odin is working…');try{const r=await fetch(`${api}/api/chat`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message})});const d=await r.json();setOutput(d.output||d.detail||'No response.')}catch{setOutput('Unable to reach the AI service.')}}return <main className="module-page"><header className="module-header"><a href="/">← Back to I AM Magnanimous</a><span className="module-status">● ODIN ONLINE</span></header><section className="module-hero"><div className="module-icon">◉</div><div><span className="eyebrow">INTELLIGENCE CORE</span><h1>AI Chat</h1><p>Talk directly with Odin and turn ideas into practical work.</p></div></section><section className="module-grid"><div className="module-panel large"><div className="panel-title"><span>ODIN CONSOLE</span><b>READY</b></div><textarea value={message} onChange={e=>setMessage(e.target.value)} placeholder="Ask Odin anything…"/><button onClick={ask}>Send to Odin →</button>{output&&<div className="module-output">{output}</div>}</div><div className="module-panel visual"><div className="pulse-ring">✦</div><h3>Agent Core</h3><p>Reasoning • Planning • Creation • Automation</p><div className="signal"><i/><i/><i/><i/><i/><i/></div></div></section></main>}

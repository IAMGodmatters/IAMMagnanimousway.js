'use client';
import {useEffect,useMemo,useState} from 'react';
import {getPlatformAuthToken} from '../lib/magnanimous-session';

const api=process.env.NEXT_PUBLIC_API_BASE_URL||'';

type Check={domain:string;has_mx:boolean;email_routing_ready?:boolean;detected_provider?:{id:string;name:string}|null;spf:boolean;dmarc:boolean;error?:string};
type Alias={id:number;domain:string;local_part:string;destination:string;address:string;status:string;steps:string[];outbound_sending_included?:boolean};
type Provider={name:string;price:string;best:string;url:string;connect?:string};

const choices=['hello','sales','support','contact','info'];
const otherProviders:Provider[]=[
 {name:'Google Workspace',price:'Paid',best:'A full Gmail business mailbox that can connect to Magnanimous AI.',url:'https://workspace.google.com/business/',connect:'google'},
 {name:'Microsoft 365',price:'Paid',best:'A full Outlook business mailbox that can connect to Magnanimous AI.',url:'https://www.microsoft.com/microsoft-365/business',connect:'outlook'},
 {name:'Zoho Mail',price:'Free / Paid options',best:'A separate custom-domain mailbox. Availability and plan terms can vary.',url:'https://www.zoho.com/mail/signup.html'},
 {name:'Proton for Business',price:'Paid',best:'A privacy-focused hosted business mailbox.',url:'https://proton.me/business/mail'}
];

async function read(r:Response){const t=await r.text();try{return JSON.parse(t)}catch{return{error:t||`Request failed (${r.status})`}}}
function cleanDomain(value:string){return value.trim().toLowerCase().replace(/^https?:\/\//,'').replace(/^www\./,'').split('/')[0]}

export default function EmailCenterClient(){
 const[token,setToken]=useState(''),[domain,setDomain]=useState(''),[local,setLocal]=useState('hello'),[destination,setDestination]=useState(''),[aliases,setAliases]=useState<Alias[]>([]),[active,setActive]=useState<Alias|null>(null),[check,setCheck]=useState<Check|null>(null),[busy,setBusy]=useState(''),[notice,setNotice]=useState('');
 const address=useMemo(()=>`${local||'hello'}@${cleanDomain(domain)||'yourbusiness.com'}`,[local,domain]);
 useEffect(()=>{const t=getPlatformAuthToken();setToken(t);try{const saved=JSON.parse(localStorage.getItem('iam_business_email_lite_draft')||'null');if(saved?.domain)setDomain(saved.domain);if(saved?.local)setLocal(saved.local);if(saved?.destination)setDestination(saved.destination)}catch{}if(t)load(t)},[]);
 useEffect(()=>{try{localStorage.setItem('iam_business_email_lite_draft',JSON.stringify({domain,local,destination,updated_at:Date.now()}))}catch{}},[domain,local,destination]);
 async function load(t=token){if(!t)return;try{const r=await fetch(`${api}/api/business-email/lite`,{headers:{Authorization:`Bearer ${t}`},cache:'no-store'}),d=await read(r);if(r.ok)setAliases(d.aliases||[])}catch{}}
 async function save(){
  if(!token){location.href='/login?returnTo=%2Fbusiness-email';return}
  if(!domain.trim()){setNotice('Step 1: type your website name, like myshop.com.');return}
  if(!local.trim()){setNotice('Step 2: pick a name, like hello or sales.');return}
  if(!destination.trim()){setNotice('Step 3: type the inbox where you already get mail.');return}
  setBusy('save');setNotice('Saving your free email plan…');setCheck(null);
  try{const r=await fetch(`${api}/api/business-email/lite`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({domain,local_part:local,destination})}),d=await read(r);if(!r.ok)throw new Error(d.error||'I could not save that yet.');setActive(d.alias);setNotice(`Good job. ${d.alias.address} is saved. Now do the little Cloudflare setup below.`);await load()}catch(e:any){setNotice(e?.message||'I could not save that yet.')}finally{setBusy('')}
 }
 async function checkDomain(){const d=cleanDomain(active?.domain||domain);if(!d){setNotice('Type your domain first.');return}setBusy('check');setNotice('Checking the mail signs on your domain…');try{const r=await fetch(`${api}/api/business-email/check?domain=${encodeURIComponent(d)}`,{cache:'no-store'}),data=await read(r);if(!r.ok)throw new Error(data.error||'I could not check the domain.');setCheck(data);if(data.email_routing_ready){setNotice('Great! Cloudflare Email Routing is turned on for this domain.');if(active&&token)await updateStatus(active.id,'domain-ready',false)}else setNotice('Not ready yet. Finish the Cloudflare steps, then press this button again.')}catch(e:any){setNotice(e?.message||'I could not check the domain.')}finally{setBusy('')}
 }
 async function updateStatus(id:number,status:string,show=true){if(!token)return;try{const r=await fetch(`${api}/api/business-email/lite/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({status})}),d=await read(r);if(!r.ok)throw new Error(d.error||'Could not update that address.');setActive(d.alias);await load();if(show)setNotice(status==='tested'?'Finished! You told Magnanimous the test email arrived. Your free receiving address is ready.':'Saved.')}catch(e:any){setNotice(e?.message||'Could not update that address.')}
 }
 async function removeAlias(id:number){if(!token)return;if(!confirm('Remove this saved setup from Magnanimous? This does not delete anything inside Cloudflare.'))return;try{const r=await fetch(`${api}/api/business-email/lite/${id}`,{method:'DELETE',headers:{Authorization:`Bearer ${token}`}});if(!r.ok){const d=await read(r);throw new Error(d.error||'Could not remove it.')}if(active?.id===id)setActive(null);await load();setNotice('Removed from Magnanimous. Your provider settings were not changed.')}catch(e:any){setNotice(e?.message||'Could not remove it.')}
 }
 function copy(value:string){navigator.clipboard?.writeText(value).then(()=>setNotice(`Copied: ${value}`)).catch(()=>setNotice('Press and hold the text to copy it.'))}
 function connectHref(p:Provider){return p.connect?`/connections?category=email&provider=${p.connect}&source=business-email`:'/connections?category=email&source=business-email'}
 return <main className="page">
  <header><a href="/">← Dashboard</a><span>FREE BUSINESS EMAIL LITE</span><a href="/inbox">Unified Inbox →</a></header>

  <section className="hero">
   <div><small>FREE TO RECEIVE • YOUR OWN DOMAIN • EASY SETUP</small><h1>Make a business email in 3 little steps.</h1><p>You pick the name. Mail goes to an inbox you already use. Magnanimous remembers the setup and checks when your domain is ready.</p></div>
   <div className="freeBadge"><b>$0</b><span>RECEIVE + FORWARD</span><i>No mailbox password needed</i></div>
  </section>

  <section className="truth"><b>Very simple:</b><span><strong>{address}</strong> receives the message → Cloudflare forwards it → <strong>{destination||'your Gmail / Outlook / Yahoo'}</strong> gets the message.</span></section>

  <section className="steps" aria-label="Three easy steps">
   <article><b className="number">1</b><div><small>YOUR WEBSITE NAME</small><h2>What is your domain?</h2><p>Example: <strong>myshop.com</strong></p><input value={domain} onChange={e=>setDomain(e.target.value)} placeholder="myshop.com" autoCapitalize="none"/></div></article>
   <article><b className="number">2</b><div><small>YOUR EMAIL NAME</small><h2>What should come before @?</h2><div className="chips">{choices.map(x=><button key={x} type="button" className={local===x?'on':''} onClick={()=>setLocal(x)}>{x}</button>)}</div><input value={local} onChange={e=>setLocal(e.target.value.toLowerCase().replace(/[^a-z0-9._-]/g,''))} placeholder="hello" autoCapitalize="none"/><p className="preview">You are making <strong>{address}</strong></p></div></article>
   <article><b className="number">3</b><div><small>WHERE SHOULD MAIL GO?</small><h2>Use an inbox you already have.</h2><p>Example: your Gmail, Outlook, or Yahoo address.</p><input type="email" value={destination} onChange={e=>setDestination(e.target.value)} placeholder="myname@gmail.com" autoCapitalize="none"/></div></article>
  </section>

  <section className="create"><div><small>YOUR FREE EMAIL</small><h2>{address}</h2><p>Receiving and forwarding can stay on the free path. Sending mail <em>from</em> this address is a separate feature and may require a sending/mailbox provider.</p></div><button onClick={save} disabled={!!busy}>{busy==='save'?'SAVING…':token?'SAVE MY FREE EMAIL →':'SIGN IN & SAVE IT →'}</button></section>
  {notice&&<div className="notice" role="status">{notice}</div>}

  {active&&<section className="setup">
   <div className="setupHead"><div><small>NOW MAKE IT LIVE</small><h2>{active.address}</h2><p>Do these one at a time. You cannot break Magnanimous by doing them.</p></div><span className={`status ${active.status}`}>{active.status==='tested'?'READY ✓':active.status==='domain-ready'?'DOMAIN READY':'SETUP NEEDED'}</span></div>
   <div className="copyRow"><button onClick={()=>copy(active.address)}>COPY {active.address}</button><button onClick={()=>copy(active.destination)}>COPY {active.destination}</button></div>
   <ol>{active.steps.map((step,i)=><li key={step}><b>{i+1}</b><span>{step}</span></li>)}</ol>
   <div className="buttons"><a href="https://dash.cloudflare.com/" target="_blank" rel="noreferrer">OPEN CLOUDFLARE →</a><button onClick={checkDomain} disabled={busy==='check'}>{busy==='check'?'CHECKING…':'CHECK MY DOMAIN →'}</button>{check?.email_routing_ready&&<button className="done" onClick={()=>updateStatus(active.id,'tested')}>MY TEST EMAIL ARRIVED ✓</button>}</div>
   {check&&<div className="health"><span className={check.has_mx?'ok':''}>{check.has_mx?'✓':'○'} Mail routing</span><span className={check.spf?'ok':''}>{check.spf?'✓':'○'} SPF</span><span className={check.dmarc?'ok':''}>{check.dmarc?'✓':'○'} DMARC</span><span className={check.email_routing_ready?'ok':''}>{check.email_routing_ready?'✓ Cloudflare Email Routing found':'Cloudflare routing not found yet'}</span></div>}
   <div className="why"><b>Why does Cloudflare open in another tab?</b><p>Your domain belongs to you. Cloudflare must let <em>you</em> approve domain and email changes on its secure website. Magnanimous keeps this checklist here so you always know what to click next.</p></div>
  </section>}

  {aliases.length>0&&<section className="saved"><div><small>SAVED BUSINESS EMAILS</small><h2>Your addresses</h2></div><div className="savedGrid">{aliases.map(a=><article key={a.id}><div><b>{a.address}</b><span>→ {a.destination}</span></div><i>{a.status==='tested'?'Ready':a.status==='domain-ready'?'Domain ready':'Setup needed'}</i><div className="mini"><button onClick={()=>{setActive(a);setDomain(a.domain);setLocal(a.local_part);setDestination(a.destination);setCheck(null);scrollTo({top:700,behavior:'smooth'})}}>OPEN</button><button onClick={()=>removeAlias(a.id)}>REMOVE</button></div></article>)}</div></section>}

  <section className="simpleExplain"><small>WHAT “FREE” MEANS</small><h2>Receive for free. Forward for free.</h2><div><p><b>Someone sends:</b> {address}</p><p><b>You receive it at:</b> {destination||'the inbox you chose'}</p><p><b>Cost from Magnanimous:</b> $0 for Business Email Lite.</p></div><p className="fine">Your domain itself may have its own yearly registration cost. Cloudflare currently offers inbound Email Routing on its free plan. Provider terms can change, so Magnanimous keeps the free feature separated from paid sending.</p></section>

  <details className="advanced"><summary>Want a full mailbox that can send too? Show other choices.</summary><div className="providerGrid">{otherProviders.map(p=><article key={p.name}><small>{p.price}</small><h3>{p.name}</h3><p>{p.best}</p><div><a href={p.url} target="_blank" rel="noreferrer">OPEN PROVIDER ↗</a><a href={connectHref(p)}>CONNECTIONS →</a></div></article>)}</div></details>

  <section className="ai"><div><small>OPTIONAL MAGNANIMOUS AI CONNECTION</small><h2>Want AI to help with the inbox that receives the forwarded mail?</h2><p>If your destination is Gmail or Outlook, connect that account securely. Then supported email actions use your own provider authorization.</p></div><a href="/connections?category=email&source=business-email">CONNECT EMAIL TO AI →</a></section>

  <footer>Business Email Lite • free receiving + forwarding path • customer-owned domain • no mailbox passwords stored • clear paid-sending boundary</footer>
  <style jsx>{`
  *{box-sizing:border-box}.page{min-height:100vh;background:#05090e;color:#edfaff;padding:24px 30px 72px;font-family:Inter,system-ui,sans-serif;background-image:radial-gradient(circle at 82% 4%,rgba(67,218,255,.12),transparent 26%),radial-gradient(circle at 8% 68%,rgba(255,190,74,.08),transparent 28%)}header{max-width:1240px;margin:auto;display:flex;justify-content:space-between;gap:14px;font-size:9px;letter-spacing:.15em;color:#7897a8}header a{color:#a9ecff;text-decoration:none}.hero{max-width:1240px;margin:24px auto 10px;border:1px solid #20506a;border-radius:24px;padding:34px;display:grid;grid-template-columns:1.4fr .45fr;gap:24px;align-items:center;background:linear-gradient(120deg,#071722,#05090e)}small{font-size:9px;letter-spacing:.16em;font-weight:900;color:#ffc969}.hero h1{font-size:clamp(42px,7vw,78px);line-height:.95;margin:10px 0 14px}.hero p,.fine,.why p,.ai p{color:#8da7b7;line-height:1.65}.freeBadge{width:210px;height:210px;margin:auto;border:1px solid #3a7188;border-radius:50%;display:grid;place-content:center;text-align:center;background:radial-gradient(circle,#0c2936,#05090e 68%)}.freeBadge b{font-size:55px;color:#74e8ad}.freeBadge span{font-size:10px;letter-spacing:.12em;color:#bceeff;font-weight:900}.freeBadge i{font-size:8px;color:#8499a5;margin-top:7px;font-style:normal}.truth{max-width:1240px;margin:10px auto;border:1px solid #315041;border-radius:14px;padding:15px 18px;background:#09140f;display:flex;gap:12px;align-items:center;flex-wrap:wrap}.truth b{color:#7debb3}.truth span{font-size:12px;color:#b7c8ce}.truth strong{color:#eefcff}.steps{max-width:1240px;margin:10px auto;display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.steps article{border:1px solid #173d51;border-radius:17px;background:#071119;padding:20px;display:flex;gap:14px;min-height:260px}.number{width:44px;height:44px;min-width:44px;border-radius:50%;display:grid;place-items:center;background:#113c50;color:#70e4ff;font-size:22px}.steps h2{font-size:22px;margin:5px 0}.steps p{font-size:11px;color:#8199a7;line-height:1.5}.steps input{width:100%;border:1px solid #29546a;background:#02070b;color:#f0fbff;border-radius:10px;padding:13px;font-size:15px;outline:none}.steps input:focus{border-color:#67e1ff;box-shadow:0 0 0 2px rgba(103,225,255,.12)}.chips{display:flex;gap:5px;flex-wrap:wrap;margin:9px 0}.chips button{border:1px solid #2c4a59;background:#08141b;color:#aec5cf;border-radius:999px;padding:7px 9px;cursor:pointer}.chips button.on{background:#69e0f7;color:#041218;border-color:#69e0f7;font-weight:900}.preview strong{color:#75e2ff}.create{max-width:1240px;margin:10px auto;border:1px solid #785f2d;border-radius:17px;background:#151108;padding:20px;display:flex;align-items:center;justify-content:space-between;gap:20px}.create h2{font-size:clamp(24px,4vw,40px);margin:5px 0}.create p{font-size:11px;color:#9e9278;max-width:730px}.create button,.buttons button,.copyRow button,.mini button{border:0;border-radius:10px;background:linear-gradient(90deg,#1aa5c8,#bf842d);color:white;font-weight:900;padding:13px 16px;cursor:pointer}.create button:disabled,.buttons button:disabled{opacity:.55;cursor:wait}.notice{max-width:1240px;margin:10px auto;border:1px solid #2a5d73;border-radius:10px;background:#071b26;padding:13px;color:#c6efff;font-size:12px}.setup,.saved,.simpleExplain,.ai{max-width:1240px;margin:12px auto;border:1px solid #173a4d;border-radius:18px;background:#071017;padding:24px}.setupHead{display:flex;justify-content:space-between;gap:18px;align-items:flex-start}.setupHead h2,.saved h2,.simpleExplain h2,.ai h2{font-size:30px;margin:5px 0}.status{border:1px solid #6c5330;border-radius:999px;padding:8px 10px;font-size:9px;font-weight:900;color:#f0c46d;white-space:nowrap}.status.tested{border-color:#337455;color:#7debb3}.status.domain-ready{border-color:#32647c;color:#78e0f8}.copyRow{display:flex;gap:8px;flex-wrap:wrap;margin:16px 0}.copyRow button{background:#0c2936;border:1px solid #2d6177}.setup ol{list-style:none;padding:0;display:grid;gap:8px}.setup li{display:flex;gap:12px;align-items:flex-start;border:1px solid #153343;border-radius:12px;background:#050b10;padding:13px}.setup li b{width:30px;height:30px;min-width:30px;border-radius:50%;display:grid;place-items:center;background:#12384a;color:#79e5ff}.setup li span{font-size:12px;line-height:1.5;padding-top:6px}.buttons{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.buttons a,.ai>a{border:1px solid #2a6178;border-radius:10px;padding:13px 16px;color:#c7f2ff;text-decoration:none;font-size:10px;font-weight:900}.buttons .done{background:#2e8056}.health{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.health span{border:1px solid #433f34;border-radius:999px;padding:8px 10px;color:#a6a093;font-size:10px}.health span.ok{border-color:#347052;color:#74e7aa}.why{margin-top:15px;border-left:3px solid #e7b456;padding:10px 14px;background:#0f0d08}.why b{font-size:11px}.why p{font-size:10px;margin:5px 0}.savedGrid{display:grid;grid-template-columns:repeat(2,1fr);gap:8px}.savedGrid article{border:1px solid #18384a;border-radius:12px;padding:13px;background:#050b10;display:grid;grid-template-columns:1fr auto;gap:10px}.savedGrid article>div:first-child{display:grid;gap:5px}.savedGrid b{color:#dff8ff}.savedGrid span{font-size:10px;color:#7e97a5}.savedGrid i{font-size:9px;color:#e4b863;font-style:normal}.mini{grid-column:1/-1;display:flex;gap:6px}.mini button{font-size:8px;padding:7px 9px;background:#0c2936;border:1px solid #2d5365}.simpleExplain>div{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.simpleExplain>div p{border:1px solid #183342;background:#050b10;border-radius:10px;padding:12px;font-size:11px}.fine{font-size:10px}.advanced{max-width:1240px;margin:12px auto;border:1px solid #3d3525;border-radius:16px;background:#0c0b08;padding:16px}.advanced summary{cursor:pointer;color:#f2c56c;font-weight:900;font-size:12px}.providerGrid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:14px}.providerGrid article{border:1px solid #2e3f46;border-radius:12px;background:#081016;padding:14px}.providerGrid h3{margin:6px 0}.providerGrid p{font-size:10px;color:#8399a5;line-height:1.5;min-height:62px}.providerGrid article>div{display:flex;gap:6px;flex-wrap:wrap}.providerGrid a{font-size:8px;color:#b8ecff;text-decoration:none;border:1px solid #294d5c;border-radius:7px;padding:7px}.ai{display:flex;justify-content:space-between;gap:20px;align-items:center}.ai p{max-width:800px;font-size:11px}footer{max-width:1240px;margin:32px auto 0;color:#66808e;font-size:9px;line-height:1.7}@media(max-width:900px){.hero{grid-template-columns:1fr}.freeBadge{width:170px;height:170px}.steps{grid-template-columns:1fr}.providerGrid{grid-template-columns:repeat(2,1fr)}.simpleExplain>div{grid-template-columns:1fr}.savedGrid{grid-template-columns:1fr}}@media(max-width:620px){.page{padding:18px 14px 60px}header{flex-wrap:wrap}.hero{padding:24px}.create,.ai,.setupHead{display:grid}.providerGrid{grid-template-columns:1fr}.steps article{min-height:0}.freeBadge{width:150px;height:150px}.freeBadge b{font-size:44px}}
  `}</style>
 </main>
}

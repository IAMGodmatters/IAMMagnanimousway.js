'use client';

import {useEffect,useRef,useState} from 'react';
import {Device} from '@twilio/voice-sdk';
import {getPlatformAuthToken} from '../lib/magnanimous-session';

const api=process.env.NEXT_PUBLIC_API_BASE_URL||'';
async function read(r:Response){const t=await r.text();try{return JSON.parse(t)}catch{return{detail:t||`Request failed (${r.status})`}}}

type Config={configured:boolean;identity?:string;token?:string;expires_at?:number;caller_id?:string;twiml_app_voice_url?:string;queue_voice_url?:string;required?:string[];free_browser_phone?:string;note?:string};

export default function Softphone(){
 const[token,setToken]=useState(''),[config,setConfig]=useState<Config>({configured:false}),[status,setStatus]=useState('Loading carrier phone…'),[number,setNumber]=useState(''),[incomingFrom,setIncomingFrom]=useState(''),[error,setError]=useState(''),[muted,setMuted]=useState(false),[online,setOnline]=useState(false),[callState,setCallState]=useState('idle');
 const deviceRef=useRef<Device|null>(null),callRef=useRef<any>(null),incomingRef=useRef<any>(null);
 useEffect(()=>{const t=getPlatformAuthToken();if(!t){location.replace('/login');return}setToken(t);setup(t);return()=>{try{deviceRef.current?.destroy()}catch{};presence(t,'offline').catch(()=>{})}},[]);
 async function authed(path:string,options:RequestInit={},active=token){const h=new Headers(options.headers||{});h.set('Authorization',`Bearer ${active}`);if(options.body&&!h.has('Content-Type'))h.set('Content-Type','application/json');return fetch(`${api}${path}`,{...options,headers:h})}
 async function presence(active:string,state:string){await authed('/api/phone/agents/me',{method:'PUT',body:JSON.stringify({status:state})},active)}
 async function getConfig(active=token){const r=await authed('/api/contact-center/softphone/config',{},active),d=await read(r);if(!r.ok)throw new Error(d.detail||'Unable to load the carrier softphone.');return d as Config}
 async function setup(active=token){setError('');try{const cfg=await getConfig(active);setConfig(cfg);if(!cfg.configured||!cfg.token){setStatus('Carrier browser phone needs the Twilio Voice SDK credentials shown below.');return}await createDevice(cfg,active)}catch(e:any){setError(e?.message||'Unable to start the carrier softphone.');setStatus('Offline')}}
 async function createDevice(cfg:Config,active:string){try{deviceRef.current?.destroy()}catch{};const device=new Device(String(cfg.token),{closeProtection:true});deviceRef.current=device;
  device.on('registered',async()=>{setStatus(`Ready as ${cfg.identity}`);setOnline(true);await presence(active,'available').catch(()=>{})});
  device.on('unregistered',()=>{setOnline(false);setStatus('Carrier phone offline')});
  device.on('error',(e:any)=>setError(e?.message||'Twilio Voice SDK error.'));
  device.on('tokenWillExpire',async()=>{try{const fresh=await getConfig(active);if(fresh.token){device.updateToken(fresh.token);setConfig(fresh)}}catch{}});
  device.on('incoming',(call:any)=>{incomingRef.current=call;setIncomingFrom(call?.parameters?.From||call?.customParameters?.get?.('From')||'Incoming caller');setCallState('ringing');setStatus('Incoming carrier call');call.on('cancel',()=>clearCall('Caller canceled'));call.on('disconnect',()=>clearCall('Call ended'));call.on('reject',()=>clearCall('Call rejected'))});
  await device.register();
 }
 function bindCall(call:any){callRef.current=call;call.on('accept',()=>{setCallState('connected');setStatus('Carrier call connected')});call.on('disconnect',()=>clearCall('Call ended'));call.on('cancel',()=>clearCall('Call canceled'));call.on('reject',()=>clearCall('Call rejected'));call.on('error',(e:any)=>{setError(e?.message||'Call error');clearCall('Call failed')})}
 function clearCall(label:string){callRef.current=null;incomingRef.current=null;setIncomingFrom('');setMuted(false);setCallState('idle');setStatus(label);if(token)presence(token,'available').catch(()=>{})}
 async function accept(){const call=incomingRef.current;if(!call)return;try{await presence(token,'busy');bindCall(call);call.accept();const sid=call?.parameters?.CallSid;if(sid)await authed('/api/contact-center/softphone/claim',{method:'POST',body:JSON.stringify({call_sid:sid})});setIncomingFrom('')}catch(e:any){setError(e?.message||'Could not answer the call.')}}
 function reject(){try{incomingRef.current?.reject()}catch{}clearCall('Call rejected')}
 async function dial(){const to=number.trim();if(!/^\+[1-9]\d{7,14}$/.test(to)){setError('Enter the destination in E.164 format, for example +14155551212.');return}const device=deviceRef.current;if(!device||!online){setError('Carrier softphone is not online.');return}setError('');setStatus(`Calling ${to}…`);setCallState('dialing');try{await presence(token,'busy');const call=await device.connect({params:{To:to}});bindCall(call);setNumber('')}catch(e:any){setError(e?.message||'Unable to place the carrier call.');clearCall('Dial failed')}}
 function hangup(){try{callRef.current?.disconnect();incomingRef.current?.reject()}catch{}clearCall('Call ended')}
 function toggleMute(){const call=callRef.current;if(!call)return;const next=!muted;try{call.mute(next);setMuted(next)}catch{}}
 async function toggleOnline(){const device=deviceRef.current;if(!device)return;if(online){await device.unregister();await presence(token,'offline').catch(()=>{});setOnline(false)}else{await device.register()}}
 return <main className="phone"><header><div><a href="/contact-center">← CX Command</a><small>MAGNANIMOUS PROFESSIONAL AGENT DESK</small><h1>Carrier Softphone</h1><p>Make and receive ordinary telephone calls in the browser through Twilio Voice SDK when the carrier credentials are configured. Free I AM browser-to-browser calling remains available separately.</p></div><div className={online?'badge live':'badge'}><i/>{online?'REGISTERED':'OFFLINE'}</div></header>{error&&<div className="error">{error}<button onClick={()=>setError('')}>×</button></div>}
 <section className="grid"><article className="dialer"><div className="screen"><small>STATUS</small><strong>{status}</strong><span>{config.caller_id?`Caller ID ${config.caller_id}`:'No carrier caller ID configured'}</span></div>{incomingFrom&&<div className="incoming"><small>INCOMING</small><strong>{incomingFrom}</strong><div><button className="answer" onClick={accept}>ANSWER</button><button className="hang" onClick={reject}>DECLINE</button></div></div>}{!incomingFrom&&<><input value={number} onChange={e=>setNumber(e.target.value)} placeholder="+14155551212" inputMode="tel"/><div className="controls"><button className="call" onClick={dial} disabled={!online||callState!=='idle'}>CALL</button><button onClick={toggleMute} disabled={callState!=='connected'}>{muted?'UNMUTE':'MUTE'}</button><button className="hang" onClick={hangup} disabled={callState==='idle'}>HANG UP</button></div></>}<button className="presence" onClick={toggleOnline} disabled={!config.configured}>{online?'GO OFFLINE':'GO ONLINE'}</button></article>
 <aside><article><small>VOICE SDK</small><strong>{config.configured?'READY':'SETUP REQUIRED'}</strong><p>{config.configured?'Short-lived signed access tokens, inbound registration and outbound TwiML are enabled.':'The web phone code is installed, but Twilio must supply the external credentials before carrier audio can register.'}</p></article><article><small>FREE FALLBACK</small><strong>I AM WEBRTC</strong><p>Use the original free browser-to-browser phone when you do not need an ordinary telephone number.</p><a href="/phone">Open Free Phone →</a></article><article><small>RECORDING</small><strong>OFF BY DEFAULT</strong><p>{config.note||'Recording stays opt-in because consent and recording laws vary by location.'}</p></article></aside></section>
 {!config.configured&&<section className="setup"><h2>Owner setup fields</h2><p>Save these through Owner Integrations / protected credentials. Secrets are never placed in public frontend code.</p><div>{(config.required||[]).map(x=><code key={x}>{x}</code>)}</div><a href="/owner-integrations">Open Owner Integrations →</a>{config.twiml_app_voice_url&&<p>Set the TwiML App Voice URL to <code>{config.twiml_app_voice_url}</code>.</p>}</section>}
 <section className="facts"><article><b>Inbound</b><span>IVR queue routes can ring up to ten registered browser agents and the first answering agent receives the call.</span></article><article><b>Outbound</b><span>Browser agents dial E.164 numbers through the configured TwiML App and workspace carrier budget controls.</span></article><article><b>Compliance</b><span>DNC and paid usage guards run server-side. Call recording is intentionally not enabled by default.</span></article></section>
 <style jsx>{`.phone{min-height:100vh;background:#050d0b;color:#eafff7;padding:30px 34px 70px;font-family:Inter,system-ui,sans-serif;background-image:radial-gradient(circle at 20% 10%,rgba(55,247,170,.09),transparent 27%),radial-gradient(circle at 90% 70%,rgba(67,115,255,.1),transparent 30%)}header{max-width:1200px;margin:auto;display:flex;justify-content:space-between;gap:20px}header a,.setup a,aside a{color:#6ff0b8;text-decoration:none}header small{display:block;margin-top:18px;color:#58987e;letter-spacing:.18em;font-size:9px}h1{font-size:clamp(48px,7vw,82px);margin:8px 0;line-height:.92}header p{max-width:760px;color:#86aa9b;line-height:1.6}.badge{height:max-content;border:1px solid #4f3337;border-radius:999px;padding:9px 13px;font-size:10px}.badge i{display:inline-block;width:8px;height:8px;border-radius:50%;background:#a95764;margin-right:7px}.badge.live{border-color:#285942}.badge.live i{background:#4ef0a7;box-shadow:0 0 12px #4ef0a7}.error{max-width:1200px;margin:18px auto;background:#39171d;border:1px solid #73313d;padding:13px 16px;border-radius:12px;display:flex;justify-content:space-between}.error button{background:none;border:0;color:#fff}.grid{max-width:1200px;margin:28px auto;display:grid;grid-template-columns:1fr 360px;gap:15px}.dialer,aside article,.setup,.facts article{border:1px solid #1f493a;background:rgba(8,23,18,.86);border-radius:22px;padding:22px}.screen{min-height:150px;background:#020706;border:1px solid #18392d;border-radius:17px;padding:22px;display:flex;flex-direction:column;justify-content:center}.screen small,aside small{color:#5da887;letter-spacing:.16em;font-size:9px}.screen strong{font-size:30px;margin:8px 0}.screen span{color:#6f9182;font-size:11px}.dialer input{width:100%;margin:18px 0;background:#06100d;border:1px solid #2c5d49;border-radius:14px;padding:17px;color:#fff;font-size:24px;letter-spacing:.04em}.controls{display:grid;grid-template-columns:2fr 1fr 1fr;gap:8px}.dialer button{border:1px solid #2d5948;background:#10251e;color:#dff8ec;border-radius:12px;padding:14px;font-weight:800;cursor:pointer}.dialer button.call,.answer{background:#55eca5;color:#052016;border-color:#55eca5}.dialer button.hang,.hang{background:#3b171c;border-color:#73313b;color:#ffced3}.dialer button:disabled{opacity:.4;cursor:not-allowed}.presence{width:100%;margin-top:10px}.incoming{margin:18px 0;padding:22px;border:1px solid #3b7b5f;border-radius:17px;background:#0a251a}.incoming small{color:#62eeb0}.incoming strong{display:block;font-size:30px;margin:8px 0 18px}.incoming div{display:grid;grid-template-columns:1fr 1fr;gap:8px}.incoming button{padding:14px;border-radius:11px;font-weight:900}aside{display:grid;gap:11px}aside strong{display:block;font-size:22px;margin:8px 0}aside p,.setup p{color:#779889;line-height:1.55;font-size:12px}.setup{max-width:1200px;margin:15px auto}.setup div{display:flex;flex-wrap:wrap;gap:7px;margin:14px 0}.setup code{background:#04100c;border:1px solid #214838;border-radius:8px;padding:8px;color:#9cf0cc}.facts{max-width:1200px;margin:15px auto;display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.facts b{display:block;color:#5ce9aa;margin-bottom:8px}.facts span{color:#799b8b;font-size:12px;line-height:1.55}@media(max-width:800px){.phone{padding:18px 13px 50px}header{display:block}.badge{display:inline-block;margin-top:12px}.grid,.facts{grid-template-columns:1fr}.controls{grid-template-columns:1fr 1fr}.controls .call{grid-column:1/-1}}`}</style></main>
}

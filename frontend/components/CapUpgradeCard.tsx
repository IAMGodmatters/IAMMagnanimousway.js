export function CapUpgradeCard({state,onUpgrade}:{state:any,onUpgrade:(id:string)=>void}){if(!state?.capped)return null;return <section aria-label="Upgrade usage"><h2>Keep going</h2><p>You reached the included premium-usage limit. Your account and work stay intact.</p><p>Add prepaid balance or upgrade for more premium AI, video, GPU, telecom and other metered services.</p><div>{(state.options||[]).map((x:any)=><button key={x.id} onClick={()=>onUpgrade(x.id)}>Upgrade: {String(x.id).replaceAll('_',' ')}</button>)}</div></section>}